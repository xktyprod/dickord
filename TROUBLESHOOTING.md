# Troubleshooting Guide - Dickord

## Бекенд не работает в EXE файле

### Ваше приложение НЕ требует отдельного бекенда!

Dickord использует облачные сервисы:
- **Firebase** - для базы данных, аутентификации
- **WebRTC** - для голосовых звонков (peer-to-peer)

Всё работает через интернет, никакой локальный сервер не нужен.

### Как проверить, что не работает:

1. **Запустите EXE файл**
2. **Нажмите F12** - откроется консоль разработчика
3. **Посмотрите на ошибки** в консоли (красный текст)

### Частые проблемы:

#### 1. Не работает с VPN 🔥 ЧАСТАЯ ПРОБЛЕМА
**Причина:** VPN блокирует доступ к Firebase серверам и WebRTC соединениям

**Решение:**
- **Временно отключите VPN** для тестирования
- Или добавьте исключения в VPN для:
  - `*.googleapis.com` (Firebase)
  - `*.firebaseapp.com` (Firebase)  
  - `*.google.com` (Firebase Auth)
  - STUN/TURN серверы для WebRTC

#### 2. Ошибка "Firebase: Error (auth/...)"
**Причина:** Проблема с Firebase конфигурацией или интернет-соединением

**Решение:**
- Проверьте интернет-соединение
- Убедитесь, что Firebase проект активен в https://console.firebase.google.com/
- Проверьте, что Authentication включён в Firebase Console

#### 2. Белый экран при запуске
**Причина:** Ошибка загрузки JavaScript файлов

**Решение:**
- Нажмите F12 и посмотрите ошибки
- Пересоберите приложение: запустите `build.bat`

#### 3. Не работает вход/регистрация
**Причина:** Firebase Authentication не настроен

**Решение:**
- Откройте Firebase Console: https://console.firebase.google.com/
- Перейдите в Authentication → Sign-in method
- Включите Email/Password authentication

#### 4. Не работают голосовые каналы
**Причина:** Проблемы с WebRTC соединением или файрволом

**Решение:**
- Проверьте, что порты UDP не заблокированы файрволом
- Убедитесь, что STUN/TURN серверы доступны
- Проверьте консоль на ошибки WebRTC

### Отладка в production:

1. Запустите EXE файл
2. Нажмите **F12** для открытия DevTools
3. Перейдите на вкладку **Console**
4. Посмотрите на ошибки (красный текст)
5. Перейдите на вкладку **Network** чтобы увидеть сетевые запросы

### Проверка Firebase подключения:

Откройте консоль (F12) и выполните:
```javascript
// Проверка Firebase
console.log('Firebase config:', firebase.app().options);

// Проверка Auth
console.log('Auth:', firebase.auth().currentUser);
```

### Если ничего не помогает:

1. Удалите приложение полностью
2. Удалите папки `dist` и `dist-renderer` в проекте
3. Запустите `build.bat` заново
4. Установите новый EXE файл
5. Запустите и нажмите F12 для просмотра ошибок

### Логи сборки:

Если проблема при сборке, проверьте:
- Все зависимости установлены: `npm install`
- Node.js версия совместима (рекомендуется v18+)
- Достаточно места на диске
